class Solution {
  int uniquePaths(int m, int n) {
    final dp = List<int>.filled(n, 1);
    for (int i = 1; i < m; i++) {
      for (int j = 1; j < n; j++) {
        dp[j] += dp[j - 1];
      }
    }
    return dp[n - 1];
  }
}
